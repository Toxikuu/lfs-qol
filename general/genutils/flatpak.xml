<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY flatpak-download "https://github.com/flatpak/flatpak/releases/download/&flatpak-version;/flatpak-&flatpak-version;.tar.xz">
]>

<sect1 id="flatpak" xreflabel="Flatpak-&flatpak-version;">
  <?dbhtml filename="flatpak.html"?>

  <title>Flatpak-&flatpak-version;</title>

  <indexterm zone="flatpak">
    <primary sortas="a-flatpak">Flatpak</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduction to Flatpak</title>

    <para>
      Flatpak allows you to download apps and run them in a sandbox. It
      downloads all of the dependencies of a given package in a sandbox and
      allows you to run the package as if it was not in a sandbox. Some
      packages only exist as a Flatpak, and some packages have so many
      dependencies that it might be better to install the Flatpak version
      instead.
    </para>

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Download: <ulink url="&flatpak-download;"/>
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Flatpak Dependencies</bridgehead>

    <bridgehead renderas="sect4">Required</bridgehead>
    <para role="required">
      <ulink url="&blfs-svn;/general/appstream.html">AppStream</ulink>,
      <ulink url="&blfs-svn;/general/dbus.html">D-Bus</ulink>,
      <ulink url="&blfs-svn;/postlfs/fuse.html">Fuse3</ulink>,
      <ulink url="&blfs-svn;/general/glib2.html">GLib</ulink>,
      <ulink url="&blfs-svn;/postlfs/gpgme.html">gpgme</ulink>,
      <ulink url="&blfs-svn;/general/json-glib.html">JSON-GLib</ulink>,
      &logind;
      <xref linkend="ostree"/> (with cURL and gpgme),
      <ulink url="&blfs-svn;/postlfs/p11-kit.html">p11-kit</ulink>, and
      <ulink url="&blfs-svn;/general/python-modules.html#pyparsing">pyparsing</ulink>
    </para>

    <bridgehead renderas="sect4">Recommended</bridgehead>
    <para role="recommended">
      <ulink url="&blfs-svn;/general/appstream-glib.html">appstream-glib</ulink>,
      <ulink url="&blfs-svn;/basicnet/avahi.html">Avahi</ulink>,
      <ulink url="&blfs-svn;/general/bubblewrap.html">Bubblewrap</ulink>,
      <ulink url="&blfs-svn;/basicnet/curl.html">cURL</ulink>,
      <ulink url="&blfs-svn;/general/libarchive.html">libarchive</ulink>,
      <ulink url="&blfs-svn;/general/libseccomp.html">libseccomp</ulink>,
      <ulink url="&blfs-svn;/basicnet/libsoup3.html">libsoup3</ulink>,
      <ulink url="&blfs-svn;/x/libXau.html">libXau</ulink>,
      <ulink url="&blfs-svn;/general/xdg-dbus-proxy.html">xdg-dbus-proxy</ulink>, and
      <ulink url="&blfs-svn;/x/xdg-desktop-portal.html">xdg-desktop-portal</ulink>
      (with <ulink url="&blfs-svn;/general/libportal.html">libportal</ulink>)
    </para>

    <bridgehead renderas="sect4">Recommended Runtime</bridgehead>
    <para role="runtime">
      Drivers for your GPU(s) for graphics APIs like OpenGL and Vulkan
      (&nvidia; or
      <ulink url="&glfs-website;/core/mesa.html">Mesa</ulink>)
    </para>

    <bridgehead renderas="sect4">Optional</bridgehead>
    <para role="optional">
      <ulink url="&blfs-svn;/gnome/dconf.html">DConf</ulink> (wanted if using
      a GNOME app via Flatpak),
      <ulink url="&blfs-svn;/general/git.html">Git</ulink>,
      <ulink url="&blfs-svn;/general/gtk-doc.html">GTK-Doc</ulink>,
      <ulink url="&blfs-svn;/general/libxslt.html">libxslt</ulink>,
      <ulink url="https://gitlab.freedesktop.org/pwithnall/malcontent">malcontent</ulink>
      (for parental features),
      <xref linkend="socat"/>
      (for tests),
      <ulink url="&blfs-svn;/general/valgrind.html">Valgrind</ulink>, and
      <ulink url="&blfs-svn;/pst/xmlto.html">xmlto</ulink>
    </para>

  </sect2>

  <sect2 role="installation">
    <title>Installation of Flatpak</title>

    <para>
      If you will be running the tests, prevent the test generation from copying
      locale files that LFS doesn't install. On LFS systems, all of the locales
      are put in one archive file.
    </para>

<screen><userinput>sed -i '92,99d' tests/make-test-runtime.sh</userinput></screen>

    <para>
      Install Flatpak by running the following
      commands:
    </para>

<screen><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

meson setup --prefix=/usr                       \
            --buildtype=release                 \</userinput><userinput revision="sysv">
            -D systemd=disabled                 \</userinput><userinput>
            -D system_bubblewrap=bwrap          \
            -D system_dbus_proxy=xdg-dbus-proxy \
            -D gtkdoc=disabled                  \
            -D tests=false                      \
            .. &amp;&amp;

ninja</userinput></screen>

    <para>
      If you removed the <parameter>-D tests=false</parameter> and want to run
      the tests, issue: <command>ninja test</command>.
    </para>

    <para>
      Now, as the &root; user:
    </para>

<screen role="root"><userinput>ninja install</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Command Explanations</title>

    &meson-buildtype-release;

    <para revision="sysv">
      <parameter>-D systemd=disabled</parameter>: This parameter disables
      Systemd support.
    </para>

    <para>
      <parameter>-D system_bubblewrap=bwrap</parameter>: Ensures the build
      system doesn't download Bubblewrap.
    </para>

    <para>
      <parameter>-D system_dbus_proxy=xdg-dbus-proxy</parameter>: Ensures the
      build system doesn't download xdg-dbus-proxy.
    </para>

    <para>
      <parameter>-D gtkdoc=disabled</parameter>: This parameter disables
      building the GTK-Doc API documentation. This is currently broken with
      gtk-doc-1.36.0, and causes the build to fail as a result.
    </para>

    <para>
      <parameter>-D tests=false</parameter>: This parameter disables tests.
      Remove this parameter if you have socat installed.
    </para>

  </sect2>

  <sect2 role="configuration">
    <title>Configuring Flatpak</title>

    <para>
      Now that Flatpak is installed, you should add a repository/remote to
      install flatpaks from. Flathub is the most popular choice. You can do
      that with:
    </para>

<screen role="nodump"><userinput>flatpak remote-add flathub https://dl.flathub.org/repo/flathub.flatpakrepo</userinput></screen>

    <para>
      You can run it as a regular user or as &root;. If you run it as a regular
      user, polkit will ask for authentication. In this case, you should ensure
      polkit works first. If all went well, no error should be output. Then you
      will be able to install flatpaks from Flathub. All installed applications
      will be in <filename
      class="directory">/var/lib/flatpak/exports/bin</filename>. You can add
      that directory to your <envar>$PATH</envar> if you wish.
    </para>

  </sect2>

  <sect2 role="content">
    <title>Contents</title>

    <segmentedlist>
      <segtitle>Installed Programs</segtitle>
      <segtitle>Installed Libraries</segtitle>
      <segtitle>Installed Directories</segtitle>

      <seglistitem>
        <seg>
          flatpak, flatpak-bisect, and flatpak-coredumpctl
        </seg>
        <seg>
          libflatpak
        </seg>
        <seg>
          /usr/include/flatpak and
          /usr/share/flatpak
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Short Descriptions</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="flatpak-bin">
        <term><command>flatpak</command></term>
        <listitem>
          <para>
            can install and run sandboxed applications via multiple repos
          </para>
          <indexterm zone="flatpak flatpak-bin">
            <primary sortas="b-flatpak-bin">flatpak</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libflatpak">
        <term><filename class="libraryfile">libflatpak</filename></term>
        <listitem>
          <para>
            contains functions used by Flatpak and other projects
          </para>
          <indexterm zone="flatpak libflatpak">
            <primary sortas="c-libflatpak">libflatpak</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
